# 基础模式设计问题清单

> 范围：`server/app/planner.py`（未配置模型时的「基础模式」确定性命令路径）
> 性质：问题记录，供后续重构参考。能力本身已较完整（约 30 个测试覆盖），问题集中在代码组织与语义路由方式。

## 背景

基础模式是未配置模型时用户可主动选择的独立档位，而非模型的降级替代。在设计原则「模型负责语义、本地工具负责确定性计算」之下，基础模式没有模型可用，因此「理解自然语言」只能靠正则与中文关键词枚举。以下问题均源于此。

## 能力边界（已决策：收缩到 A 类）

> 决策（2026-08-16）：基础模式收缩为「离线确定性语法工具」，只保留 A 类地址操作，B 类语义命令全部删除。
> 理由：B 类用正则/关键词做不了语义等价，静默答错比「请配置模型」更有害；而「无模型」场景里，基础模式唯一不可替代的价值是**零延迟、离线、100% 确定**的语法操作。

边界：

- **A 类 · 纯语法命令（保留）**：地址/范围清空与写入（`_local_address_plan`）。不需要理解含义，确定性是基础模式的绝对优势。
- **B 类 · 需要语义落地（删除）**：查找、定位、编辑、去重、极值、聚合、结果跟进、通用统计兜底，以及公式（目标单元格虽 A 类，但源范围依赖「字段→列」解析，属 B 类）。全部删除，统一响亮拒。

### 最终判定

| 命令 | 处置 |
|---|---|
| 清空/写入 A1:B2（`_local_address_plan`） | **保留**（唯一 A 类命令） |
| 写公式到指定单元格（`_local_formula_plan`） | 删除（源范围依赖字段解析） |
| 修改值（`_local_edit_plan`） | 删除 |
| 按列去重（`_local_remove_duplicates_plan`） | 删除 |
| 查找 X 的 Y（`_local_lookup`） | 删除 |
| X 在哪（`_local_location`） | 删除 |
| 极值/排名（`_local_extreme_record_response`） | 删除 |
| 聚合（`_local_aggregate_response`） | 删除 |
| 结果跟进（`_local_result_followup`） | 删除 |
| 通用统计兜底 | 删除 |

### 收缩后形态

`_local_analysis` 只做两件事：派发 `_local_address_plan`；否则返回「基础模式只支持明确的地址/范围操作，其他需求请配置模型」。

## 问题

> 注：以下 P1/P2/P3 在「收缩到 A 类」决策后大部分不再成立——P2 已提交（`1640a94`）；P1 调度链与 P3 匹配/执行耦合随 B 类命令删除而消失。仅保留作历史记录。

### P1 语义路由硬编码，调度链隐式分散

- 位置：`planner.py:1640` `_local_analysis`
- 15 个命令靠顺序 `if x is not None: return x` 串成一条链。
- 这条链的**顺序是语义依赖的**（例如「把这个结果写入」必须抢在「重新统计」之前），但没有任何地方把它显式声明出来。
- 新增一个命令需要读懂整条链才能确定插入位置。

### P2 同一语义多处重复硬编码，且已漂移

- 例：「求和」在 `planner.py:232`（`_local_formula_plan`）与 `planner.py:1413`（`_local_aggregate_response`）各写一遍，关键词还不一致（一处含「求和」、一处含「一共」）。
- 同类情况：「平均 / 最大 / 最小 / 最高」、写入动词（`WRITE_OUTPUT_MARKERS`）、结果指代词（`RESULT_REFERENCE_MARKERS`）散落多处。
- 后果：某个词在 A 命令能识别、在 B 命令不能，漂移不会报错，只会在使用中暴露。

### P3 命令匹配与执行逻辑耦合，无法单点测试

- 例：`planner.py:787` `_extreme_record_intent` 同时承担「识别极值请求 / 提取方向与名次 / 判断返回记录还是标量」三件事。
- 现有测试只能对 `create_plan` 整体输出做黑盒断言，无法单独测试某条正则的命中边界。

## 修复时需保持的事实

- 公开入口唯一：`server/app/planner.py::create_plan`（被 `main.py:65`、`test_planner.py:12` 引用）。
- 私有 `_local_*` 函数仅 `planner.py` 内部使用，测试不直接 import → 结构重构可做到测试零改动。
- 行为契约：`server/tests/test_planner.py` 约 30 个用例锁定。
- 相关文档引用：`docs/ARCHITECTURE.md:205`、`docs/DEVELOPMENT.md:249`。
