# 计划表

> 当前工作流状态追踪。状态标记：✅ 已完成 / ⏳ 待办 / 🔵 进行中。

## 基础模式收缩到 A 类（当前主线）

| 事项 | 状态 | 说明 |
|---|---|---|
| P2 聚合关键词收口 | ✅ 已提交 | commit `1640a94` |
| 能力边界决策（收缩到 A 类） | ✅ 已记录 | 见 `Issue.md` |
| 删除 B 类命令 + 孤儿 helper | ✅ 已完成 | `server/app/planner.py` -1741 行 |
| 重写测试 + 语义拒绝断言 | ✅ 已完成 | `server/tests/test_planner.py` -654 行，143 测试全绿 |
| 提交「收缩到 A 类」 | ✅ 已提交 | commit `8194e6c`（`planner.py` + `test_planner.py` + `Issue.md`） |

## 说明

- 基础模式能力边界：只保留 A 类地址/范围操作，B 类语义命令（查找/汇总/去重/比较等）统一「请配置模型」。详见 `Issue.md`。
- 收缩后 `_local_analysis` 只派发 `_local_address_plan`，其余响亮拒。
