# Excel Bro

一个本地优先的 Excel AI 助手原型。它可以读取当前工作簿，或用户指定文件夹中的多个 Excel 文件，把自然语言需求交给本地 Python 服务生成可审查的操作计划，再由 Office.js 或本地文件服务在用户确认后执行。

## 开发文档

- [架构说明](docs/ARCHITECTURE.md)：系统边界、数据流、模块职责、协议、扩展方式和技术债。
- [开发指南](docs/DEVELOPMENT.md)：环境安装、启动调试、测试、常见问题和代码评审检查表。
- [数据流程优化计划](docs/DATA_PIPELINE_OPTIMIZATION_PLAN.md)：分阶段任务、依赖关系、验收标准和后续开发状态。
- [Agent 开发约定](AGENTS.md)：供后续自动化开发工具进入仓库时直接读取的关键规则。

## 当前闭环

1. 用户选择当前工作簿，或通过系统目录选择器定位一个文件夹。
2. 插件读取可用文件和工作表，用户按本次任务进行多选。
3. 用户用中文描述分析需求。
4. 配置模型后，Excel Agent 会按需查找字段、读取区域并通过工具循环生成结构化操作计划；未配置模型时使用仅支持明确命令的“基础模式”。
5. 插件展示摘要；详细假设、风险和写入动作可以按需展开。
6. 用户确认后先预检整份计划，再执行工作表、区域、公式、格式、筛选、验证规则、表格、图表、透视表和常见对象操作。
7. 执行完成后重新读取实际工作表或磁盘文件，结果明确区分“已验证成功”“已执行但无法独立验证”和“验证失败”；若中途失败，还会展示已成功、失败和未执行的动作。
8. 验证成功的写入计划可以保存为 v2 参数化工作流；确定性查询也可以保存为固化查询，在当前工作簿或文件夹中直接本地重跑。

旧计划不需要手动迁移：服务和插件会根据工作表、写值/公式、清空、排序、筛选、表格、常用格式、边框、数据验证、冻结窗格和数据透视表动作自动生成确定性验收条件，图表则在 Office.js 创建后记录真实对象名称。排序会检查执行后的真实值顺序，筛选会读取真实 AutoFilter 条件，表格会核对真实名称、范围和表头状态；格式和验证动作会核对保存后的具体属性；当前工作簿的图表和数据透视表还会核对真实数据源、位置与字段配置。暂时无法稳定核对的条件格式和文件夹图表等动作仍明确标记为未独立验证。验收范围必须与预期矩阵尺寸一致，而且只能检查计划实际操作的工作表。

## 表格操作能力

Excel 内执行支持：

- 工作表新建、删除、切换；
- 单元格/区域值和公式写入、清空、插入、删除、复制；
- 多关键字排序、值筛选及清除筛选；
- 填充色、字体、数字格式、边框、对齐、行高列宽、自动适配；
- 合并/取消合并、冻结窗格、数据验证、条件格式；
- 超链接、批注、备注、名称区域；
- Excel 表格、图表、数据透视表、图片和形状。

文件夹模式使用 pandas 对用户已授权的“文件 ID + 工作表 ID”执行完整数据查询，使用 `openpyxl` 处理可可靠保存到 `.xlsx/.xlsm` 的动作。多文件写入先全部保存到同目录临时文件，全部成功后才创建备份并替换目标，避免中途失败留下部分新文件。Office.js 或 `openpyxl` 没有安全接口的动作会明确报错；当前不执行 VBA/宏、任意脚本或外部程序。不同 Excel 版本对备注、形状等较新的 API 支持不同，具体失败会显示在对应执行步骤。

本地聚合分析会区分直接回答和写入请求，并支持平均、合计、最大、最小等同类操作。当前工作簿按 5,000 行分块读取并由增量累加器直接计算，不会在内存中重新拼接完整数据矩阵，同时显示进度并可安全取消；文件夹查询由 pandas 读取授权工作表的完整数据，保留 Excel 中带前导零的格式化编码，并支持多文件合并、去重、结构化关联和分组。快照只用于模型上下文，确定性查询不受前 200 行快照上限限制。

计算和单元格查询回答会附带结构化结果上下文，并由任务窗格传入下一轮请求。因此“把这个平均值放在一个新子表中”“把刚才的查询结果写到汇总表 D5”等后续表达会复用上一轮结果，不会重新扩展成完整统计。上下文缺失或单数指代对应多项结果时，服务会先说明歧义。

任务窗格会在每次发送消息时重新读取当前活动工作表和选区。简繁体命令及“输出到/输出在/写到/放在”等常见表达会先归一化；“计算平均分，然后将结果输出在表格中相应位置”这类复合请求会把单个结果写入当前选区的左上角，而新工作表或多项结果仍使用结构化表格。

极值数值和极值记录会分别处理：“某字段的最大值是多少”返回最大值，“哪个对象的该字段最大”返回最大值所在行的对象。记录查询支持任意文本标识字段、并列结果、前/后 N 项、合并或分别处理多张工作表，并可作为结构化结果继续写入 Excel。

没有配置模型时，服务会对读取到的数据执行真实的本地数值统计；如果工作簿为空，则明确提示缺少数据，不会生成假的“分析计划”。开放式问答和根据业务语义自主分析仍需要配置模型。

模型规划不是一次性提示词生成 JSON，也不会在代码中硬编码行业字段。首次决策只接收用户明确选择的工作表范围、由 Excel 本地扫描识别出的字段结构、最近对话、上一轮结构化工具意图和上一轮紧凑结果；原始数据行不会进入意图识别请求。模型负责理解承接式追问；存在会改变结果的歧义时，任务窗格会显示推荐选项，并始终保留独立的“其他想法”输入框。需求明确且属于查询或统计时，模型会申请内置 `query_table` 工具，由 Excel 端在授权范围内读取完整数据并完成筛选、分组、计数、求和、平均、极值、占比和排序，只把紧凑结果与计算来源交回模型。工具字段或参数不匹配时允许模型修正一次；模型结构化输出无效时也会自动修复一次，不会直接向用户暴露协议错误。格式或写入任务则继续进入受类型系统约束且必须预览的 Agent 规划。用户要求“写公式”时会保留为 `writeFormulas` 动作，不会先在服务端算出静态值再冒充公式。无模型时只提供能力有限且明确标识的基础模式。

前端统一通过 `/api/turn` 推进一轮对话。需求确认、用户补充、本地工具修正和最终回答共享同一个 `turnId`；后端为轮次维护带过期时间和容量上限的状态，记录它当前处于澄清、工具调用、等待完成或已完成阶段。相同轮次、相同完成参数的重复请求直接返回已缓存结果，不会再次调用模型或生成第二份操作计划；已完成轮次若收到不同参数则会明确拒绝，要求创建新轮次。旧 `/api/intent` 与 `/api/plan` 暂时保留用于兼容。

内置数据工具使用结构化错误协议，区分不支持的工具、越权范围、调用上限和字段不存在等情况，并携带是否允许修正及实际可用字段。只有可修正错误才会再交给模型调整一次，确定无法恢复的错误会直接停止，避免盲目重试。统一对话接口也返回稳定的错误码、可读消息和可重试标记，任务窗格不会再把整段服务端 JSON 当作错误文案显示。

项目级读取、图片、对话、意图预览、固化工具、Agent、轮次上限和 Excel 批处理参数集中定义在 `config/capabilities.json`，后端通过 `/api/capabilities` 公布相同配置，避免前后端分别维护不一致的数字。拆分聚合默认每 25 张结果表同步一次 Office.js，减少大量拆表时的逐表往返。

长耗时请求会在对话区展示真实阶段和累计耗时，包括本地字段扫描、模型需求确认、本地数据读取与计算、回答生成或操作预览。已完成步骤会保留在当前进度卡片中，并显示工作表数量和实际扫描行数；界面不使用无法保证准确的虚假百分比。

“我的工具”保存的是结构化工作流或确定性查询模板，不是任意脚本。写入工具仍编译为新的 `AnalysisPlan` 并经过预览、确认和验收；固化查询保存字段、筛选、分组、指标、合并方式、来源和预期输出结构，重复运行时直接调用 TypeScript 或 pandas 执行器。字段、来源模式或输出结构变化时会停止本地执行并要求重新确认。诊断记录会验证正常重复运行的模型调用次数为 0。

模型接入不强制要求 API Key：配置服务地址和模型名后就会启用模型，因此 Ollama、LM Studio 等无鉴权的本地 OpenAI-compatible 网关也能直接使用。环境变量既支持项目自己的 `AI_BASE_URL`、`AI_MODEL`、`AI_API_KEY`，也兼容常见的 `OPENAI_BASE_URL`、`OPENAI_MODEL`、`OPENAI_API_KEY`；两套同时存在时优先使用 `AI_*`。

新安装不会预置 Kimi 或任何其他外部模型：任务窗格先以“基础模式”运行，并用首次引导提示用户点击顶部“添加模型”。同一个入口同时负责选择、添加和管理模型；添加成功后引导自动消失。`AI_MODEL` 仍可作为旧环境配置的默认模型，`AI_MODELS` 可以用英文逗号声明同一 OpenAI-compatible 服务地址和 API Key 下允许切换的其他模型，例如：

```env
AI_BASE_URL=https://api.moonshot.cn/v1
AI_API_KEY=你的密钥
AI_MODEL=kimi-k3
AI_MODELS=kimi-k2.6,另一个已开通的模型ID
AI_VISION_MODELS=kimi-k3,kimi-k2.6
```

点击顶部模型入口可以添加多个独立模型连接，例如分别配置 Kimi、DeepSeek、OpenAI 或本地网关，也可进入完整管理页。每个连接独立保存名称、服务地址、模型 ID、API Key 和图片能力，随后自动出现在同一个模型菜单中。保存前可发送一个不含工作簿数据的极小请求测试地址、模型和 Key；新增成功后会自动切换到新模型。编辑时空 Key 保留旧值，需要无鉴权连接时可明确清除；重复的服务地址与模型 ID 会被拒绝。独立连接保存在后端配置目录的 `model-connections.json`；损坏的配置文件会明确报错并保留原文件，不会静默覆盖。完整 Key 不会从后端读回，也不会进入聊天记录或浏览器存储。每次请求只携带服务端生成的模型选项 ID，后端解析到对应连接后再使用实际模型 ID 和 Key 调用。选择“基础模式”时不会请求外部模型。

支持视觉输入的模型会在输入框中启用图片按钮。用户可以选择、拖入或直接粘贴
PNG、JPEG、WebP 截图；一次最多 3 张，单张发送前会压缩到 4 MB 以内。图片只随
当前请求发送，不会写入本地聊天记录。所有视觉模型都必须在
`AI_VISION_MODELS` 中显式声明，系统不会根据模型名称猜测能力。

任务窗格采用持续聊天界面：顶部显示当前工作簿上下文，中间保留消息和计划记录，
底部输入框固定可用。界面统一使用 Office/Fluent 语义字号（12/14/16/20/24px）
和 Segoe UI 中文回退字体。顶部“显示方式”可在标准、加宽任务窗格之间切换，也
可以打开约 90% 的专注窗口查看长对话、结果和工具说明。专注窗口是只读视图，
所有 Excel 写入仍须回到任务窗格预览并确认。旁加载后，Excel 功能区会出现
**Excel Bro** 标签，点击 **打开 AI 对话** 即可随时唤起任务窗格。

## 目录

```text
apps/excel-addin/   React + TypeScript + Office.js 任务窗格
server/             FastAPI、本地模型适配器和计划校验
```

## 本地启动

### 1. 安装前端依赖

```powershell
npm install
```

### 2. 创建 Python 环境

```powershell
python -m venv .venv
.\.venv\Scripts\Activate.ps1
python -m pip install -r server\requirements.txt
Copy-Item server\.env.example server\.env
```

如果需要真实模型，在 `server/.env` 中填写 OpenAI-compatible 服务地址和模型名。云端服务再填写密钥；本地无鉴权网关可以不填。Kimi、其他 OpenAI-compatible 服务或本地模型网关都可以通过同一接口接入。

模型快照最多包含每张工作表的前 200 行、前 30 列；超过上限会标记截断。确定性 `query_table` 使用独立的完整数据读取路径，不把完整数据行发送给意图模型。

### 文件夹模式

在工作表选择卡片中切换到“文件夹”，点击“选择文件夹”。本地服务会递归扫描最多 100 个 `.xlsx`、`.xlsm` 文件，然后按“文件 → 工作表”展示供多选。

- 未指定目标文件的新结果写入所选目录下的 `Excel Bro 结果.xlsx`。
- 如果计划明确修改已有文件，先保存全部临时文件，随后在同一目录生成 `.excel-bro-backup-*` 备份并原子替换。
- 临时锁文件、既有备份文件和不支持的旧 `.xls` 文件不会参与扫描。
- 扫描结果包含发现文件总数与截断状态；文件夹会话有 30 分钟 TTL 和容量上限。
- 预览后文件内容变化会使来源指纹失效；当前工作簿使用全部授权来源单元格的分块指纹，文件夹计划缺少指纹也会被拒绝，旧计划必须重新生成预览。
- 当前工作簿的值、公式、仅清空内容和常用格式动作会记录一次性恢复数据，执行后可从输入栏使用“撤销”。

### 3. 启动本地服务

```powershell
npm run dev:server
```

### 4. 启动任务窗格

首次运行开发证书工具时，Windows 会请求信任本地 HTTPS 证书。

```powershell
npm run dev:addin
```

另开一个终端，把插件旁加载到 Excel：

```powershell
npm run start:excel
```

该命令会创建并打开一个临时测试工作簿，Excel Bro 只旁加载到这个工作簿；
普通“新建工作簿”不会自动继承开发旁加载。该命令仅执行 VS Code 联调所需的
旁加载，不启用依赖 Visual Studio 的 Office 直接调试器；React/CSS 仍由 Vite
热更新。停止调试：

```powershell
npm run stop:excel
```

修改过 `manifest.xml` 后，需要先停止再重新旁加载；仅刷新任务窗格不会更新功能区按钮。
若要让普通工作簿从加载项目录安装，使用 Microsoft 官方的 Windows 共享目录测试流程；
正式发布则使用 Microsoft 365 集中部署或 AppSource。

也可以直接打开 `https://localhost:3000` 在浏览器中使用内置演示工作簿调试界面；
`https://localhost:3000/focus.html` 用于单独检查专注窗口布局。窗口通信和
Office 宿主兼容性仍需在 Excel 中验证。

## Windows 本地安装包

本地安装包用于在一台 Windows 电脑上安装并进行真实 Excel 冒烟测试。它会：

- 把任务窗格和 FastAPI 服务安装到当前用户的本地应用目录；
- 安装仅用于 `localhost` 的 HTTPS 证书；
- 创建只包含加载项清单的 `\\<电脑名>\ExcelBroAddins` 本机共享；
- 把该共享注册为当前用户的 Office 可信加载项目录；
- 登录 Windows 时自动启动只监听本机的服务；
- 注册到 Windows“已安装的应用”以便卸载，并保留用户自行配置的模型连接。

创建和删除本机共享需要 Windows 管理员授权，因此安装和卸载时会各显示一次
UAC 提示。共享只授予当前安装用户读取权限，且不包含工作簿、模型密钥或其他
个人数据。安装包使用标准 Inno Setup 向导，包含欢迎、安装目录、开始菜单、
安装确认、进度和完成页面；安装目录默认是
`%LOCALAPPDATA%\Programs\Excel Bro`，用户可以在向导中修改。Windows“已安装的
应用”和开始菜单中的卸载入口均使用标准卸载器，卸载前会显示确认提示。若删除
共享所需的 UAC 被取消，卸载会中止并保留程序，允许用户重新尝试。个人模型连接
和 API Key 始终保留在 `%LOCALAPPDATA%\Excel Bro`。

构建安装包：

```powershell
npm run build:installer
```

构建机需要安装 Inno Setup 7，并确保 `ISCC.exe` 在 `PATH` 中；也可以通过
`INNO_SETUP_COMPILER` 指定完整路径。PyInstaller 只负责冻结本地服务，外层安装和
卸载生命周期由 Inno Setup 管理。

输出文件为 `dist/Excel-Bro-Setup-0.1.0.exe`。运行后完全关闭并重新打开 Excel，
首次使用依次选择 **开始 → 加载项 → 更多加载项 → 高级 → 共享文件夹**，
选择 **Excel Bro** 并点击 **添加**。这是 Office 对本机加载项目录的首次获取；
未经 Microsoft Marketplace 发布或 Microsoft 365 管理员集中部署，Windows
安装器不能代替用户完成这一步。完成一次后，普通工作簿可显示
**Excel Bro** 功能区。

该安装方式适合个人单机使用、开发和验收，不需要 Microsoft 365 企业管理员。
组织内正式分发仍应把 Web 应用部署到 HTTPS 服务器，再通过 Microsoft 365
管理中心部署清单或提交 Microsoft Marketplace。

## macOS 本地安装包

macOS 安装包由 GitHub Actions 构建（PyInstaller 不能交叉编译）：在仓库
**Actions → Build macOS installer → Run workflow** 手动触发，或推送 `v*`
标签。产物为 `dist/Excel-Bro-Setup-<版本>-<arm64|x86_64>.pkg`，按 Mac 芯片
选择对应架构。

安装包会把本地服务安装到 `/Applications/Excel Bro`，并自动完成：

- 把加载项清单放入 Mac Excel 旁加载目录
  `~/Library/Containers/com.microsoft.Excel/Data/Documents/wef`；
- 把 `localhost` HTTPS 证书信任到系统钥匙串；
- 注册 LaunchAgent，登录时自动启动只监听本机的服务；
- 安装末尾做健康检查，失败会回滚证书和自启动。

安装包未做 Apple 签名公证，首次打开会被 Gatekeeper 拦截：在 Finder 中
**右键 → 打开** 即可继续。安装后完全退出并重开 Excel，依次选择
**插入 → 加载项 → 我的加载项**，即可看到 **Excel Bro**。

卸载：在终端执行 `sudo bash "/Applications/Excel Bro/uninstall.sh"`。卸载会
移除程序、旁加载清单、证书和自启动，但保留
`~/Library/Application Support/Excel Bro` 中的个人模型配置。

## 安全边界

- 本地服务默认只监听 `127.0.0.1`。
- 模型只能返回白名单内的 Excel 动作，响应还会经过 Pydantic 和前端双重校验。
- 删除、清空和覆盖动作会在预览中显示风险提示；写入前必须由用户确认。
- 不开放 VBA/宏、网络脚本、任意 JavaScript 或外部程序执行。
